<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %> <!-- This will automatically iterate through all Users and look for a partial called _user.html.erb -->
</ul>

<%= will_paginate %>